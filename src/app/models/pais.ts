/** Modelo para los paises. */
export class Pais{

    /** Id del pais. */
    id!: number;

    /** Nombre del pais */
    nombre!: string;

    /** Siglas del pais. */
    siglas!: string;
    
    audCreatedAt!: string;

    audDeletedAt!: string;

    audUpdatedAt!: string;
}
